<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo"  bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <button id="login"> {{restaurant}} </button>
      <button id="login"> {{customer}} </button>
    </block>
  </view>
</view>
